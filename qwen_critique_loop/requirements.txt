# imgSequel — CPU-only PDF page reimagining pipeline.
# Target: AMD Ryzen 9 / 32GB / no GPU. All wheels are cross-platform.
#
# Install on a fresh box:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r qwen_critique_loop/requirements.txt

# --- core torch ---
torch>=2.2.0
torchvision>=0.17.0          # transitively required by transformers image processors

# --- diffusion + vision (all built-in transformers models, no trust_remote_code) ---
diffusers>=0.30.0
transformers>=4.45.0
accelerate>=0.34.0
safetensors>=0.4.5

# --- PDF rendering ---
pypdfium2>=4.30.0            # BSD, embedded native, no system deps (no poppler)

# --- image handling ---
Pillow>=10.0.0
numpy>=1.24.0
